{
  "name": "@carretto/mongodb",
  "version": "0.0.12",
  "private": false,
  "description": "MongoDB adapter for Zaino dataloader",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "tsc && vitest run",
    "test:ci": "vitest run --coverage --silent"
  },
  "keywords": [],
  "author": "Alessandro Magionami - @alemagio",
  "license": "MIT",
  "dependencies": {
    "@carretto/main-loader": "workspace:*",
    "tslib": "^2.4.1"
  },
  "devDependencies": {
    "@carretto/tsconfig": "workspace:*",
    "@vitest/coverage-c8": "^0.25.8",
    "graphql": "^16.6.0",
    "mongodb": "^4.11.0"
  },
  "peerDependencies": {
    "dataloader": "^2.1.0",
    "mongodb": "^4.0.0"
  },
  "files": [
    "dist"
  ]
}
